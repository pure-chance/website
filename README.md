A record of thoughts by @pure-chance, available at [pure-chance.xyz](https://pure-chance.xyz).

Run `live-server` from the project root for live preview. To deploy, run `vercel build` then `vercel deploy --prod`. To troubleshoot, check that your pages have been copied to `.vercel/output/static/`.
